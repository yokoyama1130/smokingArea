<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>喫煙所詳細画面</title>
</head>
<body>
    <h1><%= area.name %></h1>
    <div>
        <a href="/">一覧画面に戻る</a>
    </div>
    <div>
        <a href="/areas/<%= area._id %>/edit">編集する</a>
    </div>
    <form action="/areas/<%= area._id %>/delete?_method=delete" method="post">
        <button>削除する</button>
    </form>
</body>
</html>